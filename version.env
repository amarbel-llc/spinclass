# eng-versioning(7): the single source of truth for this repo's version.
# The fork's buildGoApplication auto-reads this (see flake.nix mkSpinclass);
# `just bump-version` rewrites it.
export SPINCLASS_VERSION=0.1.41
